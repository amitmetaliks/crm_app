# Python
__pycache__/
*.py[cod]
*.egg-info/
.ruff_cache/

# Node / frontend build
node_modules/
frontend/dist/
# NOTE: crm_app/public/frontend (the built PWA) IS committed so the repo is
# deployable without a yarn build (Dexciss installs as-is). Rebuild with
# `cd frontend && yarn build` when the frontend changes.

# Editors / OS
.DS_Store
*.swp
.vscode/
